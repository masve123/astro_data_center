---
// Paragraph.astro
export interface Props {
  className?: string;
  style?: string;
}

const { className, style } = Astro.props;
---

<p class={`md:text-lg text-heading-3 ${className}`} style={style}>
  <slot/>
</p>


